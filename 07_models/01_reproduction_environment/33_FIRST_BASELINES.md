# First executable baselines

Phase 3C should begin with RNA-only PCA, second-modality-only PCA/LSI as appropriate, scaled concatenation and a transparent PCA+common-clustering path from the CORE shortlist. These validate loading, identity preservation, provenance, seeds, logging and outputs.

NMF or MOFA+ follows after the deterministic path works. This order is engineering-first and predicts no performance.
