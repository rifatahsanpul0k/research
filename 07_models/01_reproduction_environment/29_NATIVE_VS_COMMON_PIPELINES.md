# Native versus common pipelines

Native runs preserve repository preprocessing, model, latent output and clustering. Common evaluation extracts Z at the documented point, validates IDs, then applies shared metrics/backends only where scientifically valid.

If a method exposes only clusters or an ambiguous latent, common embedding comparison is UNAVAILABLE rather than reconstructed from labels.
