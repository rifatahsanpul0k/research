# Preprocessing assets

Potential cached derivatives include normalized RNA, HVG masks, PCA, ATAC TF-IDF/LSI, spatial neighborhoods, coordinates, labels and feature mappings. Every asset records input checksums, fitting subset, parameters, software and output axes.

No assets are generated in Phase 3B because current matrix semantics and method-specific contracts still require resolution.
