# Latent-dimension policy

Record native default, paper-reported value and configurable range. Primary runs use a documented native setting. Equal-dimension sensitivity analyses are optional and require scientific justification.

Dimension is not forced equal automatically because changing it may alter a method's intended regularization or decoder. Any chosen value is fixed before test evaluation.
