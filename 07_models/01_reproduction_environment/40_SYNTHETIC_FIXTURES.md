# Synthetic fixtures

Tiny fixtures use explicit IDs, two or three modalities, coordinates and labels. Cases include reordered rows, duplicate IDs, missing modalities, incompatible features, NaN embeddings and a valid paired example.

They validate engineering behavior only. They are never mixed with biological datasets or reported as benchmark performance.
