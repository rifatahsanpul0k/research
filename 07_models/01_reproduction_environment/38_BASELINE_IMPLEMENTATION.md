# Baseline implementation policy

Custom baselines remain simple and transparent. Each records mathematical definition, exact library calls, preprocessing, seed, parameters, latent dimension and clustering backend. Optional convenience features do not enter silently.

Baseline code is reviewed against Phase 2B equations and tested on synthetic fixtures before project data.
