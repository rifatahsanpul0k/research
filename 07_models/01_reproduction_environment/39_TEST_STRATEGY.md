# Infrastructure test strategy

Tests cover observation order, modality pairing, feature mappings, finite embedding values, dimensions, config serialization, metric toy cases, immutable raw checksums and complete provenance. Adapter contract tests use synthetic inputs.

The current stdlib suite tests config validation, row identity, embedding shape/finite values, seeds, JSON I/O, checksums and provenance gating.
