# Schema

Inspection verified RNA 1,777 × 32,285 and ATAC 1,777 × 123,840; both are CSR `float32` with `obsm/spatial` 1,777 × 2. No dimensions were inferred from `E13` or `S1`; count/fragment semantics remain UNKNOWN.
