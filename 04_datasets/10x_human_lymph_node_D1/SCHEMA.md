# Schema

RNA and ADT are expected to be 3,359 × 18,085 and 3,359 × 31, respectively, subject to final read-only inspection. Check `obs` IDs, `var` identifiers, X encoding, and `obsm/spatial` before any pairing claim.
